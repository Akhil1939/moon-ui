/**
 * Configuration options for a badge component.
 *
 * Properties:
 * - Required:
 *   - text: Text content of the badge.
 */
export interface BadgeOptions {
  /**
   * Text content of the badge.
   */
  text: string;
}
